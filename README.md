# pi-dcp

Dynamic Context Pruning implementation for [Pi](https://pi.dev).

Under development, not ready for daily use.

## Commands

- `/dcp context` — list active, restorable compression blocks.
- `/dcp inspect <block-id>` — view an active block's replacement summary.
- `/dcp decompress <block-id>` — restore an active block's original context range.
